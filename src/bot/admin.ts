import { Bot } from 'grammy';
import { BotContext } from '../bot';

export function setupAdmin(bot: Bot<BotContext>) {
  // Admin functionality is now handled entirely within the Mini App
  // No text-based admin commands are registered.
}
